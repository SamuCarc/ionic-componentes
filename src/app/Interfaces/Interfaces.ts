export interface Componente {
        icon: string;
        name: string;
        redirectTo: string;
      
}

export interface Slide {
        img: string,
        titulo: string,
        desc: string
}